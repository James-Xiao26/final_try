node_modules
.next
dist
coverage
*.tsbuildinfo
.env
.env.local
.env.*.local
*.bom-bak
supabase/.temp/

# Throwaway local DB-inspection scripts (use the service-role key; never commit)
scripts/_*.mjs

# Installed agent skills (npx skills add ...) — local only
.agents/
.claude/skills/
skills-lock.json
