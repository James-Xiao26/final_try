{
  "name": "edgeboard",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter edgeboard-web dev",
    "build": "pnpm --filter edgeboard-web build",
    "typecheck": "pnpm --recursive run typecheck",
    "ingest": "pnpm --filter edgeboard-scripts ingest"
  },
  "workspaces": [
    "scripts",
    "web"
  ],
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
